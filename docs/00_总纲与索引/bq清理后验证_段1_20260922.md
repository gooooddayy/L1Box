# bq 清理后真机验证 · 段1（2026-09-22 21:47~21:54）

**被测产物**：`FreeBox-src/L1Box_v1.1.1_release_20260922bq.apk`
60,818,222 B ／ MD5 `57614d79…`（19:xx 装机，与机上 `base.apk` 逐字节一致）

**测试目的**：bq 做了两批死代码清理（第一刀 15 文件＋第二刀本地视频链 11 文件，共 26 个文件）
⇒ 必须真机确认**没有删坏**（首页 / 搜索 / 播放 / 设置页四条最贴近删除路径的链路）。

**判据纪律**：只做原因判定，不改代码；秒数一律以日志为准（用户口述系统性高估）。

---

## 一、日志来源

| 文件 | 体积 | 覆盖 |
|---|---|---|
| `_bq_seg1.log` | 15.5 MB | 21:47:04 → 21:55:10（宿主通道） |
| `_bq_seg1_devdump.log` | 15.2 MB | 21:47:04 → 21:54:48（设备端 `logcat -d` 灾备 dump，**同段完整**） |
| `_bn_seg/bq_seg1_2147_2154.log` | 15.2 MB | 上述 dump 的存档副本 |

设备端环形缓冲测试前已扩到 **64 MiB**，两源时间窗一致、行数差 2118 行（宿主通道多跑到 21:55:10），
**互证无丢段**。起点分界标记 `I/L1Mark: bq段1开始 2147` 已在 21:47:09.167 落下。

---

## 二、操作时间轴（我方进程 pid 28504，全程唯一进程）

| 时刻 | 事件 | 埋点原文（节选） |
|---|---|---|
| 21:53:03.699 | **冷启动** | `ActivityManager: Start proc 28504:com.github.tvbox.osc/u0a512 for top-activity {…ui.activity.SplashActivity}` |
| 21:53:04.006 | 首页 Fragment | `AndroidAutoSize(28504): …ui.fragment.HomeFragment implemented by …CustomAdapt!` |
| 21:53:05.822 | 替身层就绪 | `JarToast: 替身层加载成功(Toast/Process 已挂拦截)` |
| 21:53:12.184 | 首页分类数据 | `SortData{id='hot_gaia', name='热门电影', …}` |
| 21:53:12.235 | **搜索发起** | `搜索轮次：可搜站点=40 站点池定论=true jar装载序号=0 自动重搜=0` |
| 21:53:15.793 | 加固入口就绪 | `加固 jar 交还自身入口初始化成功，本地代理已由它接管：8ad2ef…` |
| 21:53:16.830 | 点进详情 | `诊断：详情请求 key=剧圈99 vid=63822` |
| 21:53:16.975 | 取链 换集 | `播放心跳：取链 换集 FF有广#0，额度重置` |
| 21:53:16.988 | 拿到直链 | `取链 拿到直链 url=https://vip.ffzy-plays.com/20260813/57004_e0c03114/index.m3u8` |
| 21:53:22.336 | 净化完成 | `净化 无需处理，直连 ms=5347` |
| 21:53:22.361 | 缓冲接管 | `L1Buffer: 接管：档位=3 起播门槛=1500ms 水位=300s/128MB` |
| 21:53:25.167 | **起播放行** | `起播放行 @ 缓冲=1541ms 门槛=1500ms 倍速=1.0` |
| 21:53:25.278 | **首次起播成功** | `起播 已就绪，耗时 2941ms 内核=Exo播放器` |
| 21:53:30.348 | **搜索收尾** | `搜索耗时：收尾 站点=40 命中=26 首条=1775ms 总耗时=18115ms 本轮装载增量=40 DNS(窗口内):DoH命中=70 失败=3 熔断跳过=0 直连=2110` |
| 21:53:31.375 | 第二次换线路 | `诊断：详情请求 key=天堂 vid=163211` |
| 21:53:31.628 | 拿到直链 | `取链 拿到直链 url=https://onvideo-safety.ssrcdn.com/ksc2/…` |
| 21:53:32.393 | 起播放行 | `起播放行 @ 缓冲=6560ms 门槛=1500ms 倍速=1.0` |
| 21:53:32.487 | **二次起播成功** | `起播 已就绪，耗时 645ms 内核=Exo播放器` |
| 21:54:15.745 | 播放关闭 | `QC2V4l2Driver: [avcD_9249] Closed device driver with fd: 6` |
| 21:54:19.133 | **设置页打开** | `WindowManager: NFW_setClientVisible:ActivityRecord{…SettingActivity clientVisible:true}` |

---

## 三、逐项判定

| # | 验证项 | 判定 | 依据 |
|---|---|---|---|
| 1 | **冷启动不崩、首页出内容** | ✅ 通过 | 21:53:03 Start proc（SplashActivity）→ HomeFragment → 替身层加载成功 → SortData 加载；全段零崩溃 |
| 2 | **搜索链路未受牵连** | ✅ 通过 | `命中=26`，首条 1775ms；**池已定论 ⇒ 零等待、零空态、零自动重搜** |
| 3 | **播放页（与删除代码共用资源）** | ✅ 通过 | 两次换线路（FF有广 / 超级无敌4K）均起播成功 2941ms / 645ms，内核=Exo；`播放失败`=0、`出链`=0 |
| 4 | **设置页** | ✅ 通过 | 21:54:19 SettingActivity 正常进入（121 条相关行，无异常） |
| 5 | **G1 崩溃红线** | ✅ 全 0 | `FATAL EXCEPTION`=0 ／ `ANR in`=0 ／ `Process: com.github.tvbox.osc`=0 ／ pid 28504 **不在** signal-9 退出名单（65 次 signal 9 全是其它 App 与系统回收） |
| 6 | **Q1 新提示（正在初始化播放源）** | ⚪ 未出现，**行为正确** | 因 `站点池定论=true`，`if (!isSitePoolSettled() && …)` 条件不成立 ⇒ 正常路径本就**零等待**，不该弹提示。日志里 2 次「正在初始化」是 `WexProgress` 的**「正在初始化本地代理」**（App 自带加载动画，文案不同，与 Q1 无关） |
| 7 | **P1 死链自动重取链** | ⚪ 本轮未触发 | `errReplay`／`errorFinal`／`重取` 全 0 命中 ⇒ 没遇到上游断链，属正常（观察项，遇到才算） |

### 补充观测

- **DoH 回退机制工作正常**：全段 3 次 `W/L1Dns: DoH 失败，已回退系统DNS：api.<订阅源A>.com / sdapi.<订阅源A>.com / api.<订阅源B>.com 原因=UnknownHostException`
  ⇒ 订阅源里的域名 DoH 解析不了时**自动回退系统 DNS**，未阻断任何一次请求（与 A8 结论一致）。
- **搜索窗口内 DNS 计数**：`DoH命中=70 失败=3 熔断跳过=0 直连=2110` ⇒ 被 `safeDns()` 收口后计数准确，bn 埋点按预期工作。
- **冷启首搜 18.1s 属已知固有行为**：40 个 spider 首次装载（`本轮装载增量=40`），与「DNS 变慢」无关（A8 已洗清）。首条 1775ms 正常。
- **「我的」页**：`MineFragment` / `MineActivity` 本段 **0 命中** ⇒ 本轮未访问该页（该页入口与本次删除的 26 个文件已无关联，上一轮源码核查已确认其无播放链接/本地视频入口）。

---

## 四、新发现（既有现象，**非本次清理引入**）

**现象**：spider 初始化时抛 `SQLiteCantOpenDatabaseException`

```
E/SQLiteLog(28504): (14) os_unix.c:43466: (2) open(/data/user/0/com.github.tvbox.osc/databases/tv) -
E/SQLiteDatabase(28504): Failed to open database '/data/user/0/com.github.tvbox.osc/databases/tv'.
android.database.sqlite.SQLiteCantOpenDatabaseException: Cannot open database … File … doesn't exist
    at com.github.catvod.spider.merge.Rc.G4(Unknown Source:25)
    at com.github.catvod.spider.InitOrigin.init(Unknown Source:120)
    at com.github.catvod.spider.DexNative.getLoader(Native Method)
    at com.github.catvod.crawler.ProtectedInitJar.bindDexLoader(ProtectedInitJar.java:320)
    at com.github.catvod.crawler.JarLoader.getSpider(JarLoader.java:409)
    at com.github.tvbox.osc.api.ApiConfig.getCSP(ApiConfig.java:1084)
    at com.github.tvbox.osc.viewmodel.SourceViewModel.getSearch(SourceViewModel.java:535)
    at com.github.tvbox.osc.ui.activity.FastSearchActivity.searchResult$lambda-17(FastSearchActivity.kt:704)
```

**定性：既有无害现象，与 bq 清理无关。**

跨日志对照（**26 个历史日志文件全部命中**，无一例外）：

| 日志 | CantOpen | databases/tv | 版本背景 |
|---|---|---|---|
| `_bq_seg1_devdump.log` | 8 | 16 | **本次 bq** |
| `bo_retest1.log` | 40 | 80 | bo（清理前） |
| `_bn_bo_test_host.log` | 40 | 80 | bn 时代 |
| `_bn_host.log` / `_bn_host2.log` / `_bn_a1_host.log` | 16 | 32~33 | bn 时代 |
| `a8_seg1_bn_doh_on_x3.log` | 40 | 80 | bn（A8 段1） |
| `a_c26_final.log` / `a4_cacheoff.log` / `seg_all_4.log` … | 24 | 49~50 | 更早各轮 |

判定要点：

1. **出现在 bn（清理前）与 bo（清理前）**，且出现频次与**搜索轮次**正相关（次数 ≈ 每轮 ×站点首次初始化）⇒ **与 bq 删的 26 个文件无因果关系**（删的全是 app 模块 UI/布局，调用栈全在 spider jar 侧）。
2. **调用栈 100% 落在加固 spider 内部**（`catvod.spider.merge.*` / `ProtectedInitJar` / `JarLoader`），走的是 `JarLoader.getSpider → ApiConfig.getCSP → SourceViewModel.getSearch` 正常搜索路径。
3. **不影响结果**：异常被 spider 自己 catch（紧接着就是 `D/SpiderLog:` 同级打印），本轮搜索仍 `命中=26`、两次播放全部起播成功。
4. **性质猜测**：spider 框架自带的本地库（`databases/tv`）**首次运行时不存在**，属"先试开、失败即跳过"的懒建库写法；与磁盘缓存（`exo-video-cache`）无关。

**处置建议**：不改（既有无害；改它等于动加固 spider 链路，违反「减少崩溃必须、加速锦上添花」大原则）。

---

## 五、结论

- **bq 两批死代码清理（26 个文件）真机验证通过**：首页、搜索、播放（两条线路）、设置页四条链路全部正常，**零崩溃、零 ANR**。
- **Q1 / P1 两个改动本轮未触发属正常**（池已就绪本就零等待；未遇断链），非缺陷。
- 交付层面 bq 可视为**当前最优包**（体积较 bp 再小 49 KB，行为等价，代码更干净）。
- 唯一新发现（spider 侧 `databases/tv`）经跨 26 份日志对照为**既有现象**，不需要动作。

**待办变化**：无新增必做项；P1（死链自动重取链）仍为唯一观察项，遇到才算。
