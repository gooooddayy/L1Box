# A 组验证记录 —— 播放器作用域线（A1/A2/A3）

> 环境：测试机 / `com.github.tvbox.osc` v1.1.1(vc31) / APK = **`bn`** `ff1ee2fa…`（装机 `2026-09-22 14:49:02`）
> 抓取：设备环形缓冲 64 MiB（灾备）＋ 宿主辅助通道 `_bn_a1_host.log`
> 分段存档：`_bn_seg/a1_phase1.log`（11.2 MB / 79,576 行）、`_bn_seg/a1_phase2.log`（23.4 MB / 161,748 行）
> 分界标记：`A1-START-bn`(18:22:18) → `A1-PHASE1-END`(18:28:11) → `A1-PHASE2-START`(18:28:43) → `A1-END`(18:33:26)
> **本轮未修改任何代码。**

---

## 结论总表

| 项 | 内容 | 结果 | 证据强度 |
|---|---|---|---|
| **A3** | 清空历史是否干净 | ✅ 通过 | 间接（三个源全部 `历史落库 新建`） |
| **A1-A** | 线路1 切 IJK 软解，连看 4 集不被冲掉 | ✅ 通过 | 硬（4 轮 `来源=本线路存档 plt=1`） |
| **A1-B** | 切线路2 走 Exo 默认 | ✅ 通过 | 硬（`来源=无存档重建 pl=2 plt=0`） |
| **A1-C** | 切回线路1 恢复 IJK 软解 | ✅ 通过 | 硬（`来源=本线路存档 pl=1 plt=1`） |
| **A1-D** | 全屏设置面板「切换播放器」勾选落在当前项 | ✅ 通过 | 目视（无埋点） |
| **A1-E** | 详情页「播放器」按钮勾选落在当前项 | ✅ 通过 | 目视（无埋点） |
| **A1-F** | 选 IJK 硬解/软解**真切内核** | ✅ 通过 | 硬（6 次起播内核均与选择一致，`切换失败`=0） |
| **A1-G** | 播一集后历史不重复新增、有图 | ✅ 通过 | 硬（本轮全部 `更新`、`图=有`） |
| **A1-H** | 从历史点进去能加载详情 | ✅ 通过 | 硬（2 次 `详情请求 vid=93194`） |
| **A2-1** | 详情页选 IJK 不再弹「切换失败」 | ✅ 通过 | 硬（`切换失败` 全程 0 命中） |
| **A2-2** | 解析档硬解⇄软解，退出重进后档位保持 | ✅ 通过 | 硬（重进后 `解码档=软解码` + 起播软解） |
| **G1** | 崩溃红线 | ✅ 全 0 | 硬（见下） |

---

## 一、A3 · 清空历史 ✅

- 日志中**无「清空」埋点**（`RoomDataManger.deleteVodRecordAll()` 未插桩）⇒ 这一步没有直接日志证据。
- 间接证据：清空后的 18:24 起，三个**不同 sourceKey** 的记录全部走 `历史落库 新建`：
  - `18:24:07 新建 key=天堂 vodId=165791`
  - `18:24:09 新建 key=剧圈99 vodId=93355`
  - `18:24:21 新建 key=苹果 vodId=165791`
  ⇒ 库里确为空，**清空生效**。
- ⚠ 备注：`逃出绝命街`（天堂／苹果两源）写库时 `图=空` —— 系**源站未提供封面**，非坏记录残留。
- 📌 待办：若要**直接**验证 A3，需给 `deleteVodRecordAll()` 补一条埋点（属后续批次，非本轮）。

---

## 二、A1-A / B / C · 播放器作用域三条 ✅

### A1-A 线路1（`FF有广`）切 IJK 软解，连看 4 集

| 时刻 | 集 | `播放器配置` 判定 | 起播内核 | 耗时 |
|---|---|---|---|---|
| 18:25:27 | 第01集 | `来源=无存档重建 pl=2 plt=0 解码档=硬解码` | Exo | 1925ms |
| 18:25:39 | — | ← 用户手动切 IJK 软解 | **IJK (软解码)** | 2965ms |
| 18:26:13 | 第02集 | **`来源=本线路存档 pl=1 plt=1 解码档=软解码`** | **IJK (软解码)** | 3002ms |
| 18:26:36 | 第03集 | **`来源=本线路存档 pl=1 plt=1 解码档=软解码`** | **IJK (软解码)** | 2659ms |
| 18:27:02 | 第04集 | **`来源=本线路存档 pl=1 plt=1 解码档=软解码`** | **IJK (软解码)** | 3988ms |

⇒ 连看 4 集均从**本线路存档**恢复，默认值没有把用户选择冲掉；`plt=1`（手动选过）标记正确 ⇒ `bh` 修的「解析档漏 `markUserPicked`」已在真机闭环。

### A1-B 切到线路2（`YZ有广`）

`18:27:15 → 来源=无存档重建 pl=2 plt=0 解码档=硬解码` → 起播 **Exo** 1987ms
⇒ 线路1 的 IJK 软解**没有串味**到线路2。

### A1-C 切回线路1

`18:27:38 → 来源=本线路存档 pl=1 plt=1 解码档=软解码` → 起播 **IJK 软解** 2489ms
⇒ 线路1 自己的存档**仍然在**，作用域＝「本线路本所有集」成立。

---

## 三、A1-D / E · 切换播放器入口勾选 ✅（目视）

日志无对应埋点，属**目视项**；用户报告**两个入口（全屏设置面板、详情页按钮）勾选都正落在当前项**。

> 口径提示：这一条只有**用户目视**一种证据（属阴性/主观证据），代码层未插桩，故不列入「硬证据」。

---

## 四、A1-F + A2-1 · 真切内核、不再静默失败 ✅

| 时刻 | 起播内核 | 耗时 | 说明 |
|---|---|---|---|
| 18:29:52 | IJK播放器 (软解码) | 5695ms | 进入该片 |
| **18:30:16** | **IJK播放器 (硬解码)** | 2571ms | ← 切硬解**真切了** |
| **18:30:24** | **IJK播放器 (软解码)** | 2550ms | ← 切回软解 |
| **18:31:16** | **IJK播放器 (硬解码)** | 2667ms | ← 再切硬解 |
| 18:32:07 | IJK播放器 (硬解码) | 2660ms | 重播保持硬解 |
| 18:32:21 | IJK播放器 (软解码) | 4393ms | 切回软解 |

- 全程 **`切换失败` = 0 命中** ⇒ 不再静默失败。
- 硬解／软解交替 3 轮，**每次起播内核都与选择一致** ⇒ `bh` 修的「详情页 `item >= 10` 误伤内置 100/101」已闭环。

---

## 五、A1-G · 历史不重复新增 ✅

- 阶段2 全部为 **`历史落库 更新 key=剧圈99 vodId=93194`**（0 次「新建」）⇒ 重播**不会重复新增记录**。
- `历史写入 … 图=有` ⇒ 有封面图。
- 说明：A1-G 原期望「只有 1 条」是相对「清空后首次播放」而言；本轮阶段1 已积累记录，故判据取**「新增数 = 新片数」** —— 符合。

## 六、A1-H · 从历史点进去 ✅

- `18:29:45 详情请求 key=剧圈99 vid=93194`（`vid` 非空）
- `18:32:03 详情请求 key=剧圈99 vid=93194`
⇒ 历史项能正常带 `sourceKey`+`id` 唤起详情页。

---

## 七、A2-2 · 解析档退出重进保持 ✅ ★

```
18:32:21  起播 IJK (软解码)                       ← 当前档 = 软解
18:32:32  ActivityManager: Killing 6436 … (adj 905): remove task
18:32:32  Zygote: Process 6436 exited due to signal 9 (Killed)
18:32:34  Start proc 11798 … for top-activity {SplashActivity}
18:32:38  播放器配置 片=九门 线路=FF有广 来源=本线路存档 pl=1 plt=1 解码档=软解码   ← 档位保持
18:32:42  起播 IJK播放器 (软解码) 2589ms
```

⇒ **退出重进后档位仍是用户选的「软解码」**，`plt=1` 保持，未被默认值冲掉。

---

## 八、G1 崩溃红线 —— 全程全 0 ✅

统计窗口 18:22:18 → 18:33:26（A 组全过程）：

| 关键词 | 命中 |
|---|---|
| `FATAL` | **0** |
| `Fatal signal` | **0** |
| `ANR in` | **0** |
| `JNI DETECTED` | **0** |
| `SIGSEGV` | **0** |
| `SIGABRT` | **0** |
| `tombstone` | **0** |

**进程重建（6436 → 11798）的原因已核实为系统 `remove task`（用户从最近任务划掉），`signal 9` 系 ActivityManager 主动 kill，非崩溃、非自毁。**

---

## 九、观察项（本轮**不算缺陷**，记录备查）

1. **成对重复写入**：`18:30:11`、`18:30:13`、`18:32:16` 出现间隔 <1 s 的两条相同 `历史写入 + 历史落库 更新`；阶段1 的 `18:25:35`、`18:26:13` 亦然。
   ⇒ 切换播放器/解码档会被写两次。因是**幂等更新**、不新增记录，暂无影响；若后续要清理，可在 `ProgressStore`/落库处加去重。
2. **设备内存吃紧**：`18:29:50` `lowmemorykiller: device memory 770722 free 32940 file 605995`（空闲 32~55 MB），共 6 条；但**未杀本进程**。
   ⇒ 与该机整体负载有关，作为「换低配机为什么更容易出问题」的一条佐证记录。
3. **`首页/配置` 竞态**：本轮 A 组未触发；A8 段已单独记录（就绪 5~16.5 s vs 预算 5 s）。

---

## 十、A 组剩余待做

| # | 项 | 前置 | 说明 |
|---|---|---|---|
| **A6** ★ | 水位能覆盖多远（拖 2/5/10 分钟） | 磁盘缓存**开** | **决定批四（跟随预取）生死，必须做** |
| **A7** | 同集长拖回看是否命中缓存 | 磁盘缓存**开** | 与 A6 同趟 |
| **A4** | 关掉磁盘缓存后能否正常播 | 需关开关 | 反向路径，必须有实测 |
| **A5** | 本机地址（`127.0.0.1:9978`）不得出现缓存命中 | 需播到**净化命中**的片子 | 红线项 |

---

# 追加：A6 / A7 / A5 / A4（18:38~18:45 段）

> 存档：`_bn_seg/a_c26_final.log`（46.9 MB，覆盖 18:22→18:45:21）；`_bn_seg/a_c26_final2.log`（57.0 MB，复核 18:45:21 后无新增起播）。标记 `A6-END`(18:45:19)。

## 十一、A6 · 水位能覆盖多远 ✅（口述＋日志佐证）→ **批四（跟随预取）可不做**

| 时刻 | 事件 | 说明 |
|---|---|---|
| 18:38:32 | `L1Buffer 接管：档位=3 起播门槛=1500ms 水位=300s/128MB` | **内存缓冲**水位（`L1LoadControl`），与磁盘无关 |
| 18:41:18 / 18:41:24 | `命中读取 本段 596KB/423KB 累计 0→20MB 缓存库 118→128MB` | 拖动回看**命中磁盘缓存** |
| 18:42:35~42 | `命中读取 累计 21/55/86MB 缓存库 244MB` | 大量回看命中，缓存库涨至 244MB（上限 256MB）无异常 |

- 用户口述：2/5/10 分钟拖动"都没问题"（seek 无埋点，耗时量级未留；以口述为准）。
- **语义澄清（防止以后误读）**：`水位=300s/128MB` 是**播放内存缓冲**（EXO `L1LoadControl`，设备档位决定）；磁盘缓存是另一个体系（`ExoCacheConfig`，**上限 256MB LRU 自动淘汰**，目录 `exo-video-cache`）。两者不相干。
- **⇒ 批四（跟随预取）的立项前提"10 分钟拖动会卡"不成立 ⇒ 建议不做**，省一个 APK 周期。

## 十二、A7 · 同集长拖回看命中 ✅（硬证据）

`命中读取` 埋点共 5 次（18:41:18 / 18:41:24 / 18:42:35 / 18:42:37 / 18:42:42），全部发生在拖动回看时，累计读取 0→86 MB —— **回看命中机制实证工作正常**。

## 十三、A5 · 净化链路本机地址排除 ✅（代码＋日志双证）★红线

**日志侧**：终于抓到净化命中一次（此前 8 次起播零命中）——
```
18:42:50.101  净化 开始 url=http://111.170.141.203:9090/nby/m3u8/getM3u8?…
18:42:50.281  净化 命中，走本地 HLS（免一次容器误判）ms=179
18:42:50.281  playUrl:http://127.0.0.1:9978/l1.m3u8
18:42:50.735  起播 已就绪，耗时 454ms 内核=Exo播放器
```
起播后至 18:44:05 换片，窗口内 **`命中读取` = 0 次** —— 本机地址没有产生缓存读。

**代码侧**（`player` 模块，不在 app 模块，之前 grep 搜错范围）：
- `ExoMediaPlayer.java:113`：`boolean useCache = ExoCacheConfig.isEnabled() && ExoCacheConfig.isCacheableUrl(path);` —— **排除在播放器主路径生效**；
- `ExoCacheConfig.isCacheableUrl()`（`ExoCacheConfig.java:88`）：`://127.0.0.1` / `://localhost` / `://[::1]` / `://0.0.0.0` → `false`，另排除 `file:`/`content:`/`asset:`/`rtmp`/`rtsp`；
- 排除理由（注释原文）：净化/网盘代理端点**每集都是同一个固定 URL**，若进缓存，Exo 按 URI 做 key ⇒ **第 2 集会直接命中第 1 集的残留（"换集还在播上一集"）**，是必然回归而非概率问题。

## 十四、A4 · 关磁盘缓存播放 ⚠ **日志无证据 —— 未做，待补**

- 复核 `_bn_seg/a_c26_final2.log`（57.0 MB）：**18:45:21 之后 `L1Play`/`L1Buffer`/`L1Cache`/`playUrl` 全部 0 命中** ⇒ 关缓存后没有发生任何起播。
- 上半段（缓存开）5 次起播全有 `L1Buffer 接管` ＋ `磁盘缓存已就绪`，缓存确实全程开着。
- **待补（1 分钟）**：设置页关「磁盘缓存」→ 随便播一集看 30 秒。判据：能播、无崩溃、日志无 `L1Buffer 接管`/`L1Cache` 行（关闭生效的反向证据）。
- 顺带核实**关闭即时生效**链路存在：`SettingActivity.kt:340 → ExoCacheConfig.setEnabled(newConfig)`，无需重启。

## 十五、本段 G1 崩溃红线

18:22:18 → 18:45:21 全程：`FATAL`/`Fatal signal`/`ANR in`/`JNI DETECTED`/`SIGSEGV`/`SIGABRT`/`tombstone` **全 0**。

---

# 追加二：A4 补测（19:10）→ ✅ 通过，A 组 8 项全部闭环

> 存档：`_bn_seg/a4_cacheoff.log`（78.9 MB / 544618 行，覆盖 18:22→19:11）；标记 `A4-DONE-cacheoff`(19:11:31)。

## 十六、A4 · 关磁盘缓存后播放 ✅

| 判据 | 结果 |
|---|---|
| 能播 | ✅ 19:10:25 起播成功（**苹果·超级无敌4K**，起播 **627ms**，Exo），之后 `缓冲升档 @ 37s`，30 秒+ 无异常 |
| 不崩 | ✅ G1 红线全 0（FATAL/Fatal signal/ANR in/JNI DETECTED/SIGSEGV/SIGABRT/tombstone） |
| 关闭生效（反向证据） | ✅ 窗口内（18:45:21→19:11）`L1Cache`=**0**、`磁盘缓存已就绪`=**0**、`命中读取`=**0** —— 对比上半段（缓存开）5 次起播全带这些行 |

> 注：窗口内出现的 `L1Buffer 接管` 是**播放内存缓冲**（与磁盘缓存无关，见 §十一 语义澄清），不作为"缓存还开着"的证据。

## 十七、⚠ 附带观察：19:10:22 一次播放失败（热播影视·本源免费切勿付费购买！）

```
19:10:14.179  取链 拿到直链 url=http://43.248.96.62:9090/nby/m3u8/getM3u8?…
19:10:14.459  净化 命中，走本地 HLS ms=280 → playUrl:127.0.0.1:9978/l1.m3u8
19:10:21.876  （7.4s 无起播）兜底 回退原始直连（同一 URL）
19:10:22.102  兜底 降级到 IJK播放器（第 1/1 档）
19:10:22.239  播放失败：出链 reason=自动兜底试尽，转为明确提示
19:10:24.851  用户已手动切到「苹果」线路 → 起播 627ms 正常
```

- 与 09-22 上午 404 现象链**同一族**：兜底链在"回退原始直连"上打转（URL 同源，多半同样死），**没有"重新取链"档** —— 这是 **P1 的又一例支持证据**（当日第 2 例）。
- 但仍只有个例、且**换线路立即可播** ⇒ 维持"P1 待拍板"定位，不改变本轮结论。

## 十八、A 组最终状态（09-22 全部闭环）

| 项 | 结果 | 一句话 |
|---|---|---|
| A1 作用域 A~H | ✅ | 线路存档/不串味/两入口勾选全对 |
| A2 真切内核+档位记忆 | ✅ | 切换失败=0；重进档位保持 |
| A3 坏历史清空 | ✅ | 清空后全 `新建`、有图 |
| A4 关缓存播放 | ✅ | 能播 627ms、红线全 0、关闭生效 |
| A5 本机地址排除 | ✅ | 净化命中一次＋`ExoMediaPlayer:113` 代码双证 |
| A6 水位覆盖 | ✅ | 2/5/10 分钟全过 ⇒ **批四不做** |
| A7 回看命中 | ✅ | `命中读取`×5 硬证据 |
| A8 变慢疑问 | ✅ | DoH 无责；失败=就绪竞态（5s 预算 vs 就绪最长 16.5s） |

**G1 崩溃红线全程（18:22→19:11）全 0。**
