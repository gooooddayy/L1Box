# L1Box 工作区清理审计报告（2026-09-14）

> **本报告只做扫描与建议，尚未删除或移动任何文件。** 等你确认后我才会动手。
> 原则：**宁可少清，绝不误删**；大件优先"**移出工作区**"（可逆、零丢失），而非直接删除。

---

## 〇、为什么要清：卡顿归因

| 位置 | 体积 |
|---|---|
| `<本机>/WorkBuddy` 全部会话 | **8.8 GB** |
| └ 本会话 `2026-08-19-14-43-05` | **8.3 GB**（占 94%） |
| └ 其余 47 个会话合计 | 0.5 GB |

**结论：卡顿几乎全部来自本工作区。** 清完这一个就够，其余会话不用碰。

本工作区 8.3GB 的构成：

| 类别 | 体积 | 性质 |
|---|---|---|
| 旧 APK（两处归档） | **4.2 GB** | 历史产物，最大头 |
| 历史 logcat 大日志 | **1.78 GB** | 抓取现场 |
| `_cleanup_20260911/`（崩溃取证+截图） | **1.4 GB** | 旧现场 |
| `FreeBox-src/` 源码与工程 | **0.7 GB** | ★ 必需 |
| `dl/`（JDK11） | **0.31 GB** | ★ 必需 |
| `_tmp_exo/` + `_probe/` 等零碎 | **0.03 GB** | 中间产物 |

---

## 一、候选清单（按"安全度"分四档）

### 第 1 档 · 历史日志（低风险）

| 文件 | 体积 | 处置建议 |
|---|---|---|
| `logcat_al_20260914.log` | 423.82 MB | 保留（am 崩溃根因的原始证据） |
| `logcat_am_20260914.log` | 261.67 MB | 保留（同上） |
| `logcat_aq_20260914.log` | 253.15 MB | **保留**（最近一次已实证的基线） |
| `logcat_ar_20260914.log` | 84.31 MB | **保留**（ar 待测，还没用） |
| `logcat_ag_20260913_archived.log` | 176.47 MB | 可清（文件名已标 archived） |
| `logcat_ak_20260913.log` | 168.53 MB | 可清（ak 结论已并入总纲，且为"运气版"） |
| `logcat_aj_20260913.log` | 152.05 MB | 可清（结论已固化进 `crash_analysis_aj`） |
| `logcat_ap_20260914.log` | 149.01 MB | 可清（拒载回归结论已固化） |
| `logcat_ai_20260913.log` | 80.94 MB | 可清（结论已固化进 `crash_analysis_ai`） |
| `logcat_ak_idle_archived.log` | 71.70 MB | 可清（文件名已标 archived） |

- **可清合计 ≈ 799 MB**（ag / ak / aj / ap / ai / ak_idle）
- 保留 4 个后仍有完整证据链（09-14 的 al→am→aq→ar）

### 第 2 档 · 旧 APK（最大头，4.2 GB）

| 位置 | 数量 | 体积 |
|---|---|---|
| `FreeBox-src/_archive_old_builds/` | 51 个 | **3.1 GB** ← **在 Gradle 工程内**，索引负担最重 |
| `_archive_old_builds/`（根目录） | 18 个 | **1.1 GB** |

**建议：移出工作区，不删除。** 目标位置 `<本机>/WorkBuddy\_archive_l1box_apk\`（工作区外、可随时查回）。
若确认只留里程碑，可保留 **ab（速度基线）/ ak（0 崩基准）/ aq（拒载归零）/ ar（当前交付）** 四个，其余移出。

> 已核查：`build_l1box.sh` / `check_l1box_regressions.sh` / `sign_l1box.sh` **均未引用** `_archive*` 目录
> → 移走不会断链。

### 第 3 档 · `_cleanup_20260911/`（1.4 GB，需你拍板）

| 子目录 | 体积 | 内容 | 建议 |
|---|---|---|---|
| `crash/` | **1.3 GB** | `crash_capture*.log`、`crash_full*`、41 个 tombstone 目录（09-11 崩溃取证现场） | 结论已进 `问题解决全记录`；可移出或删 |
| `shots/` | 72 MB | 过程截图 | 可清 |
| `logs/` | 33 MB | 旧日志 | 可清 |
| `tools/` | 23 KB | 小工具脚本 | 先留 |

### 第 4 档 · 零碎中间产物（约 30 MB）

| 文件/目录 | 体积 | 说明 | 建议 |
|---|---|---|---|
| `_tmp_exo/` | 23 MB | 反编译 Exo 2.18.7 的中间产物（水位方案已定稿） | 可清 |
| `_probe/` | 6.6 MB | 探测脚本 + 截图 | 可清 |
| `_fs.png` | 0.96 MB | 单张截图 | 可清 |
| `build_*_091*.log`（9 个） | ~0.25 MB | 09-10/09-11 历史构建日志 | 可清 |
| `_aq_search.log` | 0 B | 空文件 | 可清 |
| `_sub_probe/` | 15 KB | `aigo.txt` 等（含 native 二进制 `v8_size` 证据） | **保留**（体积可忽略） |

---

## 二、明确保留（防误删清单）

以下**一律不动**：

| 类别 | 具体 |
|---|---|
| 源码工程 | `FreeBox-src/` 全部（**仅**其内部 `_archive_old_builds/` 属第 2 档） |
| 构建环境 | `dl/`（JDK11）、`.guserhome2/`（Gradle 缓存）、`gradle/` |
| 记忆 | `.workbuddy/`（含 `memory/` 全部日记） |
| 文档 | 15 份专题 `.md` + 本次新增 `L1Box_知识总纲_20260914.md` |
| 脚本 | `build_l1box.sh`、`sign_l1box.sh`、`check_l1box_regressions.sh`、`check_l1box_compat.sh`、`validate_home_config.py` |
| 产物 | `FreeBox-src/L1Box_v1.1.1_release_20260914ar.apk`（当前交付） |
| 待测日志 | `logcat_ar_20260914.log` |
| 其它 | `L1Box_五项功能自查清单.html`、`gen_icon.py`、`make_icon.py`、`_sub_probe/` |

---

## 三、三档方案（请选一个）

| 方案 | 动作 | 回收 | 工作区剩余 | 风险 |
|---|---|---|---|---|
| **A · 保守（推荐）** | 第 1 档清 6 个日志 + 第 2 档 APK **移出工作区** + 第 4 档零碎 | **≈ 5.0 GB** | ≈ 3.3 GB | 极低（移出不删） |
| **B · 只清日志** | 仅第 1 档清 6 个日志 | 0.8 GB | 7.5 GB | 最低 |
| **C · 彻底** | A + 第 3 档 `_cleanup_20260911/` 一并处理 | **≈ 6.4 GB** | ≈ 1.9 GB | 中（旧取证现场不再留存） |

> 无论选哪个，**删除动作都会分批执行、每批后核对，且不碰第二节保留清单**。
> 若你有 D 盘/移动硬盘等外部位置更合适，告诉路径，我把 APK 与取证现场搬到那里。

---

---

## 四、执行记录（2026-09-14 20:5x，方案 A · 保守）

**执行原则**：**全部采用"移出工作区"，未删除任何文件**——可逆、零丢失。

| 批次 | 动作 | 结果 |
|---|---|---|
| 1 | `_archive_old_builds/`（根，18 个 APK）→ 移出 | ✓ |
| 1 | `FreeBox-src/_archive_old_builds/`（51 个 APK）→ 移出 | ✓ |
| 2 | 6 个历史日志（ag/ak/aj/ap/ai/ak_idle）→ 移出 | ✓ |
| 3 | `_tmp_exo/`、`_probe/`、`_fs.png`、`_aq_search.log`、9 个 `build_*_091*.log` → 移出 | ✓ |

**移出位置**：`<本机>/WorkBuddy\_archive_l1box_apk\`
```
_archive_l1box_apk/
├── rootside_archive_old_builds/   18 个 APK   1.1 GB
├── insrc_archive_old_builds/      51 个 APK   3.1 GB
├── logs/                           6 个日志   0.8 GB
└── misc/                          13 项       约 30 MB
```

**结果**

| 指标 | 清理前 | 清理后 |
|---|---|---|
| 工作区体积 | 8.3 GB | **3.5 GB** |
| 回收 | — | **4.8 GB** |

**关键资产核对（18 项，全部在位）**：源码工程、`dl/`、`.guserhome2/`、`.workbuddy/`、5 个脚本、当前产物 `L1Box_v1.1.1_release_20260914ar.apk`、ar 待测日志、4 份证据日志（al/am/aq/ar）、两份新文档、`_sub_probe/`。

**剩余 3.5GB 归因**：源码 0.7GB + `dl/` 0.31GB + 保留日志 1.02GB + **`_cleanup_20260911/` 1.4GB（属 C 方案，本次未动）**。

**随时可恢复**：所有移出物按原目录结构分类存放，需要哪个直接搬回原路径即可。

> 说明：本次未做真删除。若你希望进一步腾出磁盘（那 6 个日志约 800MB），说一声我再删。

